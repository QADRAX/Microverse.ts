{
  "name": "@luarizer/integration-examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Integration-style examples and tests that consume only @luarizer/luarizer (final-client style).",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@luarizer/luarizer": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
