{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://luarizer.local/schemas/luarizer-def-manifest.json",
  "title": "LuarizerDefManifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "output"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "output": { "type": "string", "minLength": 1 },
    "headerNote": { "type": "string" },
    "aliases": {
      "type": "array",
      "items": { "$ref": "#/$defs/alias" }
    },
    "classes": {
      "type": "array",
      "items": { "$ref": "#/$defs/class" }
    },
    "globals": {
      "type": "array",
      "items": { "$ref": "#/$defs/global" }
    },
    "luaHooks": {
      "type": "array",
      "items": { "$ref": "#/$defs/luaHook" }
    }
  },
  "$defs": {
    "alias": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "definition"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "definition": { "type": "string", "minLength": 1 }
      }
    },
    "param": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "luaType"],
      "properties": {
        "name": { "type": "string" },
        "luaType": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "method": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "params": { "type": "array", "items": { "$ref": "#/$defs/param" } },
        "returns": { "type": "string" },
        "callStyle": { "type": "string", "enum": ["tablePayload", "singleValue"] }
      }
    },
    "class": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "fields": { "type": "array", "items": { "$ref": "#/$defs/globalField" } },
        "methods": { "type": "array", "items": { "$ref": "#/$defs/method" } },
        "emitSingleton": { "type": "boolean" }
      }
    },
    "globalField": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "luaType"],
      "properties": {
        "name": { "type": "string" },
        "luaType": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "global": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "fields"],
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "fields": { "type": "array", "items": { "$ref": "#/$defs/globalField" }, "minItems": 1 }
      }
    },
    "luaHook": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "paramName", "payloadLuaType"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "paramName": { "type": "string", "minLength": 1 },
        "payloadLuaType": { "type": "string", "minLength": 1 },
        "description": { "type": "string" }
      }
    }
  }
}
