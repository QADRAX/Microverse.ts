import { MicroverseLua } from './microverseLuaNamespace.js';

/**
 * @deprecated Use {@link MicroverseLua} — runtime is built-in; `createWasmRuntime` is no longer exposed here.
 */
export const Microverse = MicroverseLua;
